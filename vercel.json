{
  "version": 2,
  "installCommand": "npm install postgres --legacy-peer-deps",
  "builds": [
    { "src": "api/**/*.ts", "use": "@vercel/node" },
    { "src": "index.html", "use": "@vercel/static" }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1.ts" }
  ]
}